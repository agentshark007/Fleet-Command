"""Entry point script for Fleet Command.

This script simply imports and runs the main() function from app.py,
allowing the game to be started with `python main.py`.
"""

from app import main

if __name__ == "__main__":
    main()
